# 当前配置状态

> 文档更新时间：2026-03-25

## 数据库连接配置

### 当前连接方式
- **连接类型**：SSH 隧道 + MySQL 8.0
- **远程服务器**：linyubo.top
- **本地端口**：3307
- **远程端口**：3306
- **数据库用户**：root
- **数据库密码**：mysql_h8TCKC
- **数据库名**：reading_reflection

### SSH 隧道命令
```powershell
ssh -i "$env:USERPROFILE\.ssh\id_rsa" -f -N -L 3307:127.0.0.1:3306 root@linyubo.top
```

### 后端 .env 配置
```env
# 服务配置
SERVER_PORT=8080

# 数据库配置（SSH 隧道）
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=mysql_h8TCKC
DB_NAME=reading_reflection

# JWT 配置
JWT_SECRET=your-jwt-secret-key-change-me

# 智谱 AI 配置
ZHIPU_API_KEY=dc9bf8defa2e47dd95a612fd16e9b3f0.hy4T83eTbaVcWKtY
ZHIPU_MODEL=glm-4-flash
```

## 数据库表结构

### 已创建的表

| 表名 | 用途 | 状态 |
|------|------|------|
| `users` | 用户账户 | ✅ 已创建 |
| `books` | 书籍管理 | ✅ 已创建 |
| `chapters` | 章节管理 | ✅ 已创建 |
| `paragraphs` | 段落管理 | ✅ 已创建 |
| `prompt_templates` | 提示词模板 | ✅ 已创建 |
| `questions` | 题目管理 | ✅ 已创建 |
| `user_settings` | 用户设置 | ✅ 已创建 |
| `practice_records` | 练习记录 | ✅ 已创建 |

### 提示词模板表

`prompt_templates` 表包含以下字段：
- `id` - 主键
- `user_id` - 用户 ID（NULL 表示系统模板）
- `name` - 模板名称
- `question_type` - 题型（名词解释/意图理解/生活应用）
- `content` - 提示词内容
- `is_default` - 是否默认模板
- `is_system` - 是否系统模板

### 系统预设模板

已插入 3 个系统预设模板：
1. **名词解释 - 标准模板**
2. **意图理解 - 标准模板**
3. **生活应用 - 标准模板**

**注意**：由于字符集问题，模板内容在数据库中可能显示为乱码，但功能正常。

## 后端服务状态

### 当前状态
- **运行状态**：✅ 运行中
- **监听端口**：8080
- **数据库连接**：✅ 正常（通过 SSH 隧道）
- **API 健康检查**：http://localhost:8080/health

### 启动命令
```bash
cd backend
go run main.go
```

## 前端配置

### 开发服务器
- **启动命令**：`npm run dev`
- **访问地址**：http://localhost:5173
- **API 代理**：http://localhost:8080/api

### 环境变量
```env
VITE_API_BASE_URL=http://localhost:8080/api
```

## 测试账户

### 当前可用账户
- **用户名**：10002
- **密码**：123456
- **用户 ID**：0f966ea6-ff60-4d27-9fc8-cd072e5b40d2

**注意**：该用户为新注册用户，书架为空。

## 已知问题

### 1. 数据丢失问题
- **问题描述**：之前创建的书籍和用户数据不在远程数据库中
- **可能原因**：之前数据保存在本地 MySQL（Docker 容器），容器已删除
- **解决方案**：重新添加书籍数据

### 2. 提示词模板乱码
- **问题描述**：系统预设模板在数据库中显示为乱码
- **原因**：MySQL 客户端字符集问题
- **影响**：不影响功能使用，可通过前端正常显示和编辑
- **解决方案**：通过前端界面重新创建系统模板

## 部署配置

### 服务器信息
- **服务器 IP**：114.132.47.245
- **域名**：linyubo.top
- **管理面板**：1Panel
- **前端目录**：/opt/1panel/www/sites/readrecall/index/
- **后端容器**：readrecall-backend
- **数据库容器**：1Panel-mysql-OHb5

### 部署检查清单
- [x] 后端服务运行正常
- [x] 数据库连接正常
- [x] SSH 隧道已建立
- [x] 前端可访问
- [ ] 书籍数据需要重新添加

## 快速开始

### 本地开发环境启动

1. **启动 SSH 隧道**
```powershell
ssh -i "$env:USERPROFILE\.ssh\id_rsa" -f -N -L 3307:127.0.0.1:3306 root@linyubo.top
```

2. **启动后端服务**
```bash
cd backend
go run main.go
```

3. **启动前端开发服务器**
```bash
npm run dev
```

4. **访问应用**
- 前端地址：http://localhost:5173
- 登录账户：10002 / 123456

---

**最后更新**：2026-03-25 12:00
**更新内容**：数据库配置、提示词模板功能、SSH 隧道配置
